<!-- hero shot -->
<!-- <div class="container"> -->
  <div class="row">
    <!-- <div class="col lg"> -->
      <div class=""><%= link_to image_tag('logo.png', class:'col s11 m5 l3 logo'), root_path %></div>
    <!-- </div>   -->
  </div>
  <div class=""><%= image_tag("heroshot-xl.jpg", class:'col l12 responsive-img hero', :alt => "Trekabilly")%>

    <div class="container hero-search">
      <div class="">
          <!-- search form -->
          <%= form_tag '/search', method: 'get' do %>
            <%= label_tag "search" %>
            <%= text_field_tag :name %>
            <%= submit_tag "search" %>
          <% end %>
      </div>
    </div>
  </div>
    
 

  
    <h1 class="headline col s12 center">Why Trekabilly?</h1>
    <p class="center">Our community has both locals and tourists to give you the best of both worlds.
Our community has both locals and tourists to give.</p>
    <h2 class="sub-head center">Browse some of our categories</h2> 

  <div> 

    <% @categories.each do |cat| %>
      <ul style="list-style-type: none ">
        <li><h3><%= cat.name %></h3></li>
        <% post_count = 0 %>
        <% @posts.each do |post| %>
          <% if post.category_id == cat.id %>
            <h3><%= link_to post.title, post %></h3>
            <p>Category <%= post.category_id %></p>
            <p><%= link_to image_tag(post.image, class:'posts-image'), post %></p>
            <p><%= post.content[0...50]+"..." %></p>
            <br>
            <% post_count +=1 %>
          <% end %>
        <% break if post_count == 3 %>
        <% end %>
      </ul>
    <% end %>
  
<!-- </div> -->